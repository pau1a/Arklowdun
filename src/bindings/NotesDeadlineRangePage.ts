// This file mirrors the generated binding from ts-rs for NotesDeadlineRangePage.
// It is maintained manually for test environments where bindings are unavailable.

import type { Note } from "./Note";

export interface NotesDeadlineRangePage {
  items: Note[];
  cursor?: string;
}
